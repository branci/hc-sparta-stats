CREATE TABLE "MATCH"(
    "MATCH_ID" BIGINT PRIMARY KEY,
    "DATE" DATE NOT NULL,
    "OPPONENT" VARCHAR(30) NOT NULL,
    "SPARTA_GOALS" INTEGER NOT NULL,
    "OPPONET_GOALS" INTEGER NOT NULL,
    "SPARTA_SHOTS" INTEGER NOT NULL,
    "OPPONET_SHOTS" INTEGER NOT NULL,
    "SPARTA_PENALTY" INTEGER NOT NULL,
    "OPPONET_PENALTY" INTEGER NOT NULL,
    "HOME" VARCHAR(30) NOT NULL,
    "BEST_PLAYER" VARCHAR(30) NOT NULL,
    "SEASON" INTEGER NOT NULL);

CREATE TABLE "PLAYERS" (
    "PLAYERID" INT PRIMARY KEY,
    "NAME" VARCHAR(30) NOT NULL,
    "AGE" INTEGER,
    "HEIGHT" INTEGER,
    "WEIGHT" INTEGER,
    "PLAYERNUM" INTEGER,
    "POSITION" INTEGER);

CREATE TABLE "STATS" (
    PLAYERID INT,
    MATCHID INT,
    GOALS INT,
    ASSISTS INT,
    PENALTY_MINUTES INT,
    SHOTS INT,
    HITS INT);

CREATE TABLE "GOAL" (
    "GOAL_ID" BIGINT ,
    "MATCH_ID" BIGINT NOT NULL,
    "PLAYER_NAME" VARCHAR(20) NOT NULL,
    "MINUTE" INTEGER NOT NULL);

CREATE TABLE "ASSIST" (
    "ASSIST_ID" BIGINT PRIMARY KEY,
    "MATCH_ID" BIGINT NOT NULL ,
    "PLAYER_NAME" VARCHAR(20) NOT NULL,
    "GOAL_ID" BIGINT NOT NULL);

CREATE TABLE "PLAYED" (
    "MATCH_ID" BIGINT NOT NULL,
    "PLAYER_NAME" VARCHAR(20));
